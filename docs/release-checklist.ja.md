# リリース前チェックリスト

学生がMac/Windowsで導入しやすい状態かを確認するためのチェックリストです。

## 起動

- [ ] Macで `Gemma4_12B_全部起動.command` が起動する
- [ ] Macで `Gemma4_12B_Web.command` が起動する
- [ ] Windows ZIP版で `Gemma4_12B_All_Start.bat` が起動する
- [ ] Windows ZIP版で `Gemma4_12B_Web.bat` が起動する
- [ ] Windows MSIでデスクトップに `TOMOS AI Web UI` ショートカットが作られる
- [ ] Windows MSIでスタートメニューに `TOMOS AI` フォルダーが作られる
- [ ] スタートメニューから `TOMOS AI Web UI`、`TOMOS AI 全部起動`、`TOMOS AI 重い処理を停止` を選べる
- [ ] Windows MSIのショートカットが `.bat` 直指定ではなく `Gemma4_12B_Launcher.exe` を指している
- [ ] Windows MSI版は Program Files 内の `.bat` を探さなくても起動できる
- [ ] 起動後、ブラウザーで `http://127.0.0.1:54876/` が開く
- [ ] 設定画面にアプリ版、モデル状態、利用モデルが表示される

## GitHub Release配布

- [ ] `bash scripts/make-release-archives.sh` でMac/Windows ZIPを作成できる
- [ ] `dist/TOMOS_AI-vX.X.X-mac.zip` が作成される
- [ ] `dist/TOMOS_AI-vX.X.X-windows.zip` が作成される
- [ ] 署名証明書を登録したMacで `TOMOS_AI-vX.X.X-mac.pkg` を作成し、Apple公証を通している
- [ ] GitHub Actionsで `TOMOS_AI-vX.X.X-windows.msi` が作成される
- [ ] ZIPに `.git/`、`ComfyUI/`、`.venv/`、`.venv-asr/`、モデルファイルが含まれていない
- [ ] GitHub ReleaseにMac/WindowsのZIP、PKG、MSIを添付している
- [ ] 古いReleaseを削除せず残している
- [ ] 学生向け案内に、使うReleaseのURLとOS別インストーラー名を記載している

## モデル

- [ ] 通常画面の選択肢が `自動（おすすめ）`、`標準AI`、`コード作業`、`高性能AI` の4つだけになっている
- [ ] 通常画面では用途名を先に表示し、内部モデル名は各おすすめカード内で小さく確認できる
- [ ] 取得済みの非推奨モデルだけが`不要なモデルを削除`に表示される
- [ ] `標準AI` がチャット、翻訳、資料検索、教材パックの標準として使われる
- [ ] `コード作業` は専用AIが取得済みなら使い、未取得なら標準AIへ一度だけ戻る
- [ ] `高性能AI` は画像添付時だけ使われる
- [ ] 翻訳用のカードが重複せず、翻訳は標準AIの用途に含まれる
- [ ] Enterpriseモデルが学生向けの通常画面に表示されない
- [ ] 実験モデルと制限を弱めたモデルが自動選択されない
- [ ] 旧設定で非表示モデルを選択していた場合だけ、安全な自動選択への移行案内が表示され、通常モデルの既存値は維持される
- [ ] 高性能AIが未取得で画像を添付した場合、送信せず追加案内が表示される
- [ ] モデル自動、速度、精度の切り替えがチャットフォームから使える
- [ ] 標準AIのコンテキストが通常設定で8K以上32K以下になる

## 資料検索・送信先

- [ ] 資料検索の対応形式がTXT、Markdown、PDFだけと案内される
- [ ] CSVとExcelが利用可能とは表示されない
- [ ] 検索結果に原文根拠が付く
- [ ] Enterprise接続は初期OFFで、管理者許可が必要と表示される
- [ ] Enterprise接続時は `企業サーバー送信`、通常時は `ローカル処理` と表示される
- [ ] 企業資料という理由だけで企業サーバーへ自動送信されない

## 基本チャット

- [ ] `おはよ` など短い会話で自然に短く返る
- [ ] `英訳して` / `日本語にして` で全文がチャットに返る
- [ ] 回答下に所要時間、モデル、用途、モードが表示される
- [ ] ユーザー発話とGemma回答のコピーボタンが機能する

## Web検索・天気

- [ ] Web検索ONで外部リンクに↗アイコンが出る
- [ ] Web検索ONで新しい情報を検索結果付きで答える
- [ ] `新潟の天気は？` でOpen-Meteoの結果が表示される
- [ ] 現在地を保存した後、`今日の天気は？` でブラウザ現在地由来だと分かる
- [ ] 現在地を使わない場合は既定地または地名指定で動作する

## フォルダー操作

- [ ] フォルダーを作成し、チャットがフォルダー配下に表示される
- [ ] フォルダー編集からローカルフォルダーを指定できる
- [ ] ローカルフォルダーのファイル一覧が表示される
- [ ] `helloと記載されたtextを保存して` のような単純保存が成功する
- [ ] 簡単なWebプログラム作成で `index.html` が保存され、プレビューできる

## 学習セット

- [ ] フォルダー編集で、そのフォルダーに使う学習セットを選べる
- [ ] 回答下の「修正して学習」から保存先学習セットを選べる
- [ ] 学習セットの中身を確認できる
- [ ] 保存済みの正しい回答を編集できる
- [ ] 学習用ファイルを書き出した後、その時点ではモデル自体が変わらないことが分かる

## 画像

- [ ] 画像をフォームの＋から添付できる
- [ ] 画像をクリップボードから1回だけ貼り付けられる
- [ ] ComfyUIが起動済みならチャットから画像生成できる
- [ ] 画像生成後にPCが重くなりすぎない

## 開発者確認

- [ ] JSヘルパーテスト一式が通る
- [ ] Python構文チェックが通る
- [ ] Safariで手動確認できる
- [ ] README.ja.md / README.en.md の手順が現在の起動ファイルと一致している
